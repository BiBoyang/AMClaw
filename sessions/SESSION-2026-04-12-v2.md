# Session 2026-04-12 (续)

## 今天目标

- PR2/PR3（Memory 消费接入 + 观测回归）
- T1-T5 安全/错误/日志/日报/会话 五环节推进

## 完成内容

### PR2/PR3 — Memory 消费接入 + 观测回归（已完成）

- `load_business_context_snapshot` 切换到 `search_user_memories`
- 命中回写 + 结构化日志 + 失败降级
- AgentRunTrace / AgentTraceIndexEntry / to_markdown 增加 memory 统计
- 3 个回归测试（用户隔离、预算裁剪、无记忆退化）

### T1 — 安全边界门禁（已完成）

- `read_article_archive` 路径校验：output_path 改用 `resolve_path` 走 workspace 边界检查
- SSRF 防护：`normalize_url` 新增 `is_private_host` 检查（10/127/172.16-31/192.168/localhost 等）
- Prompt 注入防护：`sanitize_for_prompt` 清理控制字符 + 截断
- 归档回复不再暴露绝对路径（改用相对路径）
- 新增 2 个安全测试（archive 路径越界、内网 URL 拒绝）

### T2 — thiserror 统一错误分层（已完成）

- 新增 `thiserror = "2"` 依赖
- `TaskStoreError` 枚举（InvalidUrl / PrivateNetworkUrl / UnsupportedScheme / Validation / Database）
- `ToolError` 枚举（PathTraversal / EmptyPath / FileExists / Io）
- `normalize_url` / `resolve_path` / `create_file` 已替换为类型化错误
- pipeline 的 `PipelineTaskError` 已有手动 impl Error，保持不变

### T3 — 日志收口（已确认无需修改）

- 全量扫描后确认：所有模块已通过 `emit_structured_log` 统一输出
- 仅 `logging.rs` 基础设施通道和 `main.rs` 日报 summary 使用 println/eprintln，均为合理用途

### T4 — 微信摘要回传（已完成）

- `build_daily_report_query_reply` 改为回传完整 markdown 内容（而非仅 summary + 路径）
- 不再暴露 `markdown_path` 服务器路径
- 超过 3800 字符自动截断并标注总数
- 定时推送功能已存在且完整

### T5 — 会话持久化（已确认无需修改）

- 审查后确认：`persist_session_snapshot` + `restore_persisted_sessions` + `task_store` 支持已完整
- 启动时自动恢复、消息后自动持久化

## 改动文件

- `src/agent_core/mod.rs`：Memory 消费接入 + Trace 扩展 + sanitize + 测试
- `src/task_store/mod.rs`：thiserror + SSRF 防护 + is_private_host + 测试
- `src/tool_registry/mod.rs`：thiserror + 路径校验加固 + 测试
- `src/chat_adapter/mod.rs`：日报回传修复 + 测试
- `src/pipeline/mod.rs`：无实质改动
- `Cargo.toml`：thiserror 依赖
- `PLAN.md` / `NEXT-STEPS.md`：状态同步

## 校验

- [x] `cargo check`
- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets --all-features`（仅 `suppress_memory` dead_code 为预期）
- [x] `cargo test`（171 passed）
