# Session 2026-04-21

## 今天目标

- 回到 AMClaw 问题清单，优先收口高危稳定性与安全边界问题。

## 完成内容

### 1. 高危稳定性与安全修复

- 修复 `tool_registry` 路径边界校验：引入真实路径解析，阻断符号链接越界读写。
- 修复 SSRF 判定：引入 DNS/IP 级私网识别，修复 `fc/fd` 前缀域名误杀，并覆盖 IPv4 变体地址。
- 修复 pipeline 抓取链路：`HTTP Client` 改为复用实例，避免每次抓取重建。
- 修复消息去重事务分支：去除重复消息分支显式 rollback，降低重复处理风险。

### 2. P1/P2/P3 收口与回归

- P1：`TaskStore::open` 增加 `WAL` + `busy_timeout` + busy 重试；补并发写入回归测试。
- P2：reporter 改为 SQL 时间范围过滤后再 limit，并修复 UTC/本地时区边界判定与排序语义。
- P3：CI 新增 `cargo fmt -- --check`、`cargo clippy -- -D warnings`、`cargo test` 质量门禁。
- 完成一次提交并推送：`0022944`（`main` 分支）。

## 改动文件

- `src/tool_registry/mod.rs`：路径真实解析与 symlink 越界防护、相关测试。
- `src/task_store/mod.rs`：WAL/busy_timeout、SSRF 判定增强、范围查询、排序修复、并发与安全测试。
- `src/pipeline/mod.rs`：HTTP client 复用（含 redirect/no-redirect 两套客户端）。
- `src/chat_adapter/mod.rs`：适配 `Pipeline::new -> Result`。
- `src/reporter/mod.rs`：日报/周报范围查询与 UTC 跨日边界测试。
- `.github/workflows/trace-eval-compare.yml`：新增 fmt/clippy/test 门禁。
- `src/agent_core/mod.rs`：清理 clippy 告警（`Default derive`、`too_many_arguments` allow）。

## 校验

- [x] `cargo check`
- [x] `cargo fmt -- --check`
- [x] `cargo clippy --all-targets --all-features -- -D warnings`
- [x] `cargo test`

## 评测摘要

- **trace_eval 命令**: `cargo run --bin trace_eval`
- **报告路径**: `notes/agent-eval/reports/TRACE-EVAL-REPORT.md`
- **baseline 文件**: `notes/agent-eval/baselines/EVAL-BASELINE-SAMPLES-2026-04-18.md`

### 本次核心指标

| 指标 | 数值 | 判定 |
| --- | --- | --- |
| success_rate | N/A | N/A |
| fallback_rate | N/A | N/A |
| context_drop_rate | N/A | N/A |
| tool_success_rate | N/A | N/A |
| recovery_success_rate | N/A | N/A |

### 评测结论

- [ ] PASS — 无 regression，可继续
- [x] WARN — 有波动，需关注
- [ ] FAIL — 存在 regression，需修复

### 判定依据

（本次未执行 trace_eval 全量评测；已完成针对高危修复点的定向回归与编译/静态检查，建议下一轮补跑 trace_eval 并对比基线。）

## 风险与后续

- 风险：当前 CI 仅一个 workflow，trace gate 仍为 soft gate；生产级并发模型（poll loop 解耦 worker）尚未实施。
- 下一步：推进 `chat_adapter` 与任务处理解耦（worker 队列、重试与限流），并补 restricted 运行时策略门禁。
