# Session 2026-04-19

## 今天目标

- 落地微信长回复分段发送与长输入“处理中”回执
- 修正分段发送下的 `reply_sent` 日志语义
- 审阅当日其余改动并同步文档口径

## 完成内容

### 1. 微信回复链路体验增强（分段 + 回执）

- 在 `chat_adapter` 增加分段与回执常量：
  - `WECHAT_REPLY_CHUNK_MAX_CHARS = 1200`
  - `PROCESSING_ACK_MIN_INPUT_CHARS = 180`
  - `PROCESSING_ACK_TEXT = "收到，处理中，稍后给你完整回复。"`
- 在 `send_generated_reply` 中，Agent 生成前按阈值发送“处理中”回执，降低长任务空等感。
- `send_reply_text` 改为分段发送：`split_reply_into_chunks` 后逐段调用 `send_text_message`，并输出 `reply_chunk_sent` 日志。
- 定时日报推送链路复用分段发送，避免长日报单条发送失败。
- 新增 4 条测试：
  - `split_reply_into_chunks_short_text_single_chunk`
  - `split_reply_into_chunks_long_text_multi_chunks`
  - `split_reply_into_chunks_preserves_full_content_when_joined`
  - `should_send_processing_ack_threshold_behavior`

### 2. 可观测语义修正（reply_sent）

- 修复分段发送场景下“总是打 `reply_sent`”的语义偏差：
  - 全部分段发送成功：`reply_sent`（含 `sent_chunk_count`、`all_sent=true`）
  - 中途失败：`reply_partially_sent`（含 `sent_chunk_count`、`all_sent=false`）
- 保留失败段 `reply_send_failed`（带 `chunk_index/chunk_total`）用于定位。

### 3. 其余改动 review 与文档对齐

- 已 review 其余已改文件：
  - `src/agent_core/mod.rs`
  - `src/config.rs`
  - `src/context_pack.rs`
  - `notes/context-memory/CONTEXT-PACK-C3-PLAN-2026-04-18.md`
- 已完成两处文档口径对齐：
  - `README.md` 中分段前缀由 `(i/n)` 对齐为实现实际的 `（i/n）`
  - `CONTEXT-PACK-C3-PLAN` 中优先级值对齐代码：`SessionText 65`、`UserMemories 86`

## 改动文件

- `src/chat_adapter/mod.rs`：分段发送、处理中回执、日志语义修正、新增测试
- `src/agent_core/mod.rs`：trace 路径回写、Context 组装与 memory budget 配置化演进（review）
- `src/config.rs`：`agent` 新增 context/memory 相关配置字段（review）
- `src/context_pack.rs`：context 总预算与 section 优先级调整（review）
- `README.md`：同步用户可见行为与分段前缀口径
- `notes/context-memory/CONTEXT-PACK-C3-PLAN-2026-04-18.md`：同步 section 优先级与开关说明
- `sessions/SESSION-2026-04-19.md`：当天收尾记录

## 校验

- [x] `cargo check`
- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets --all-features`
- [x] `cargo test`

## 评测摘要

- **trace_eval 命令**: `cargo run --bin trace_eval`
- **报告路径**: `notes/agent-eval/reports/TRACE-EVAL-REPORT.md`
- **baseline 文件**: `notes/agent-eval/baselines/EVAL-BASELINE-SAMPLES-2026-04-18.md`

### 本次核心指标

| 指标 | 数值 | 判定 |
| --- | --- | --- |
| success_rate | N/A（本次未重跑 trace_eval） | N/A |
| fallback_rate | N/A（本次未重跑 trace_eval） | N/A |
| context_drop_rate | N/A（本次未重跑 trace_eval） | N/A |
| tool_success_rate | N/A（本次未重跑 trace_eval） | N/A |
| recovery_success_rate | N/A（本次未重跑 trace_eval） | N/A |

### 评测结论

- [x] PASS — 本次改动以发送链路与文档对齐为主，基础回归通过
- [ ] WARN — 有波动，需关注
- [ ] FAIL — 存在 regression，需修复

### 判定依据

- `cargo fmt/check/clippy/test` 全通过。
- 分段与回执新增测试通过。
- `reply_sent`/`reply_partially_sent` 语义已区分，日志可观测性提升。

## 风险与后续

- 风险：分段发送中途失败会停止后续段（当前策略优先避免乱序，可能导致信息不完整）。
- 风险：`PROCESSING_ACK_TEXT` 当前为常量，后续若需多语言/个性化需配置化。
- 下一步：进入“完整 SessionState + 恢复动作映射（retry/replan/ask_user/fallback）”主线实现。

---

## 收尾更新（第二步：周报能力）

### 本次完成

- 新增周报生成能力（按 ISO 周聚合 archived 任务）：
  - 支持输出 `data/reports/weekly-YYYY-WW.md`
  - 支持周报摘要文本（微信查询/推送复用）
- 新增周报命令路由：
  - `周报`
  - `周报 YYYY-WW`
  - `weekly report` / `weekly report YYYY-WW`
- 新增周报自动推送：
  - 复用 `[scheduler].report_to_user_id` 与 `daily_run_time`
  - 每周一同一时刻触发（按配置 timezone）
  - 发送链路复用分段发送，超长自动拆分
- 会话路由兼容：
  - `WeeklyReportQuery` 明确走 `SessionEvent::Noop`，不进入会话合并缓冲

### 涉及文件

- `src/reporter/mod.rs`：新增 `WeeklyReportOutput`、`current_week`、`generate_weekly_for_week` 与周报渲染/摘要逻辑
- `src/scheduler/mod.rs`：新增 `WeeklyReportSchedule` 与每周本地周报生成入口
- `src/command_router/mod.rs`：新增 `RouteIntent::WeeklyReportQuery` 与解析函数
- `src/chat_adapter/mod.rs`：接入周报命令处理、周报查询回复、每周自动推送
- `src/session_router.rs`：新增 `WeeklyReportQuery` 的 Noop 分支
- `README.md`：补周报能力、命令与调度说明

### 新增测试

- `command_router::tests::weekly_report_command_is_supported`
- `reporter::tests::weekly_report_is_generated_for_archived_tasks`
- `scheduler::tests::weekly_report_schedule_is_built_from_config`
- `chat_adapter::tests::weekly_report_query_builds_reply`

### 本轮校验

- `cargo fmt --check` ✅
- `cargo check` ✅
- `cargo test` ✅（257 passed）

### 风险与后续

- 风险：周报推送与日报推送共用同一时间配置，后续如需分离应补独立 weekly 配置项。
- 风险：周报当前按 `tasks.updated_at` 聚合，后续若业务口径变为 `archived_at`，需补字段与迁移。
- 下一步：进入第三步“完整 Session State（prompt + trace 一体）”。

---

## 收尾更新（第四步：Mature Plan & Execute + 恢复映射）

### 本次完成

- 在 `agent_core` 中引入集中恢复策略映射表：`default_recovery_for_failure`，统一维护 `failure_kind -> action/max_attempts/escalate_to`。
- 在 `RuntimeControllerState` 增加 `recovery_kind_counts`，按 kind 计数，超阈值自动升级，形成最小防循环保护。
- 在 `handle_recorded_failure` 接入统一映射与升级逻辑，恢复动作不再分散在多处硬编码分支。
- `RecoveryTrace` 补齐观测字段：
  - `original_action`
  - `effective_action`
  - `action`（兼容旧消费方，值同 `effective_action`）
  - `escalated`
- `record_recovery_attempt` 改为显式接收 `effective_action`，确保 trace 记录的 action 与实际执行动作一致。
- `trace_eval` 的 `RecoveryAttemptTrace` 同步新增 `original_action/effective_action`，并使用 `#[serde(default)]` 兼容旧 trace。

### 涉及文件

- `src/agent_core/mod.rs`：恢复映射、防循环、恢复观测字段、相关回归测试
- `src/bin/trace_eval.rs`：恢复字段兼容解析与 recovery 统计测试
- `sessions/SESSION-2026-04-19.md`：当天收尾记录补充

### 新增/强化测试

- `agent_core::tests::transient_failure_retry_then_replan`
- `agent_core::tests::low_value_observation_replan_then_ask_user`
- `agent_core::tests::recovery_loop_guard_prevents_infinite_escalation`
- `agent_core::tests::trace_records_recovery_attempt_action_outcome`
- `trace_eval::tests::recovery_success_rate_computation_with_mixed_attempts`
- `trace_eval::tests::report_parser_tolerates_missing_recovery_fields`

### README 同步判定

- 本轮主要是恢复策略与可观测性内核改进，未新增用户命令或配置入口。
- 现有 README 对外能力描述未发生冲突，因此本轮不需要额外 README 文案变更。

