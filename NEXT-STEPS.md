# NEXT-STEPS

当前执行说明，基于现有 `PLAN.md` 继续推进，但不替代 `PLAN.md`。

## 当前优先级

当前已完成聊天流 M1、链接流 M1、状态查询与最小任务消费骨架。

下一阶段重点是把 `tasks` 从“可写、可查”推进到“可消费、可流转”。

建议目标：

1. 把占位处理器升级为真正的内容处理流水线
2. 写入更有用的归档产物，而不只是占位 Markdown
3. 为失败任务补更明确的错误来源和手动恢复路径
4. 为后续日报与调度留稳定接口

## 范围边界

当前明确暂不做：

- 修改 `PLAN.md`
- `tokio` 迁移
- 真正的网页抓取与正文抽取
- 定时任务与日报生成
- 模式策略治理

## 实现顺序

1. 提升 URL 规范化和链接元信息提取
2. 让 `pipeline` 产出更有意义的结构化文件
3. 增加失败任务的查询和恢复入口
4. 保持微信主链路和聊天流行为不退化
5. 完成后再考虑日志分级与 `tokio` 迁移

## 当前已完成

### command_router

- 聊天流：`..` / `!!` / 超时合并
- 链接流：自动识别 URL 与 `收藏 <url>` 入库
- 命令流：`状态 <task_id>`、`最近任务`、`重试 <task_id>`

### task_store

- SQLite 初始化
- `message_dedup`
- `inbound_messages`
- `articles`
- `tasks`
- 最近任务查询
- 任务状态查询
- 任务重试

### pipeline

- 扫描并消费 `pending` 任务
- 生成本地占位归档文件
- 推进任务状态到 `archived` 或 `failed`

### chat_adapter

- 微信入口分流为聊天流 / 链接流 / 查询命令
- 入站文本先落库再处理
- 轮询期间顺带消费 `pending` 任务
- 保留现有简单回复和文件型 Agent 能力

## 完成标准

下一阶段完成后，至少要满足：

- 发链接后任务能被后台消费
- `状态 <task_id>` 能看到状态推进
- `最近任务` 能反映最新处理结果
- 聊天流、链接流、查询命令三条入口互不冲突
