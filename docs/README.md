# Docs

本目录存放仍有参考价值、但不需要放在仓库根目录的长期文档。

## 子目录

- `architecture/`：架构设计、runtime 分层、实现计划等中长期设计材料。
- `roadmap/`：未来功能路线、优先级和分层归属建议。

## 说明

- 当前可运行能力与命令以根目录 `README.md` 为准。
- 当前开发状态与下一步以根目录 `PLAN.md` / `NEXT-STEPS.md` 为准。
- 已完成的任务计划、review 和 session 记录放在 `notes/archive/` 或 `sessions/`。
