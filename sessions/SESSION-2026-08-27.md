# Session 2026-08-27

## 今天目标

- 基于全项目架构复审结论，执行 8 项建议 + 1 个 blocker 的修复（Wave 1 四路并行 + Wave 2 单路）。

## 完成内容

### 0. 全项目复审（前置）

- 5 个簇独立调查 + 交叉核对：架构骨架健康（无环/无反向依赖、深模块到位）；债务集中在"声明 vs 实现"漂移与两套未接线的失败词表。
- 唯一 blocker：`planning_stall_rate` 指标因运行时/L1 词表无映射对真实数据恒为 0。

### 1. B1 + trace_eval 加固

- 运行时→L1 失败词表映射表落地（幂等，旧 L1 数据行为不变），死指标复活（演示数据 0/2 → 2/2）。
- 新增 `trace_version` 白名单校验 + 核心字段缺失计数（`Trace Schema Health` 报告小节）——当场抓到 4 条老 trace 缺 `failures` 字段。
- sidecar/旧 markdown 双向兼容；gate 判定逻辑零改动。

### 2. retriever 去重

- 抽 `semantic_common.rs` 共享骨架（cosine / fallback 构造 / embed+打分），hybrid -261 行、semantic -169 行；semantic 缺失的 fallback metadata 补齐（漂移修复）。
- `cached_embedding` 4 个 helper 吞错统一为 warn 日志 + cache miss。
- `RetrieveResult.hit_count/dropped_count` 改如实注释（有外部 struct 字面量触点，不删字段）。

### 3. task_store 清死 + 文档追平

- 删 `get_pending_task`；`list_pending_tasks` / `list_archived_tasks` / `clear_user_session_state` / `clear_embedding_cache` / `embedding_cache_stats` 降 `#[cfg(test)]`。
- 模块 AGENTS.md/CLAUDE.md 追平（记忆治理、session state v2、lease/worker、embedding 缓存、pending chunks）；`daily_reports` 死表标注废弃不删。
- 顺手：`TaskStoreError` 死变体收敛、孤儿 doc 注释、重复建表语句、`govern_memory_write` 写入失败补 warn 日志、DefaultHasher 风险注释。

### 4. agent_core 簇四项

- auth/transport 判定三处拷贝收敛到 `classify_env_failure` 单一来源；provider fallback 口径有意变宽（非 401 的鉴权失败也触发切换）。
- prompt 契约 action 列表从 `available_tool_descriptions` 动态生成（system prompt + ResponseContract），修复 `task_store_db_path=None` 时广告不可用工具的漂移。
- 删除 mode_policy 死门禁 `check_tool_action`（真实白名单在 `map_llm_plan`），收缩为纯 URL 门禁；顺带清理 `AgentCore.mode` 死字段。
- 小项：`is_agent_command` 单源化；删除 agent 失败后才生效的硬编码 echo fallback；task_executor panic 分支补 `mark_task_failed`。

### 5. W2：日报/周报双触发统一 + 推送补发语义

- scheduler 线程成为唯一生成触发点；查询与推送改为读优先（`{root}/reports/daily-{day}.md` 快照存在即读，缺失才现生成）——消除双写与"查询即生成"的写副作用。
- 推送部分失败未发段落落 `pending_chunks`（与回复路径同机制），已发段不重复、未发段不丢失；两个 80 行推送方法收敛为 `push_scheduled_report` + 薄包装。
- scheduler"到点判断"两份实现合一；`report_to_user_id` 缺失不再阻止生成线程启动。

## 校验

- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets --all-features`（0 warning）
- [x] `cargo test` 全量（505 passed, 0 failed，含 doc_mirrors / scope_markers）
- [x] trace_eval 真实数据 + 演示数据实跑验证（blocker 修复前后对比）

## 评测摘要

- trace_eval 自身口径变更（失败映射 + schema health），gate 判定未动；compare 冒烟 PASS。
- 日报/周报改为快照语义：当天新增归档在下次生成前不进查询结果（README 已说明）。

## 风险与后续

- 风险：定时推送链路属"大改动"边缘，**待用户扫码回归**：到点只生成一次、部分失败补发不重复、查询快照语义、无推送目标时只生成不推送。
- 用户可见变化：`日报`/`周报` 返回最近生成快照；鉴权失败提示文案微调。
- 遗留可选：agent_core/AGENTS.md "不绕过模式策略"措辞更新；Wilson 第二步（gate 消费置信界）等样本量。
- 硬前置不变：真实 trace 样本积累 → 第一批语义失败标注。
