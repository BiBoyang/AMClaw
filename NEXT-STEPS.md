# NEXT-STEPS

当前执行说明，基于现有 `PLAN.md` 继续推进，但不替代 `PLAN.md`。

## 当前优先级

当前已完成聊天流 M1、链接流 M1、状态查询、最小任务消费、公众号浏览器抓取骨架与人工补录闭环。

下一阶段重点是把 `tasks` 从“可写、可查”推进到“可消费、可流转”。

建议目标：

1. 稳定浏览器抓取在真实任务流中的表现
2. 继续提升公众号正文提取质量
3. 为待人工补录任务补更顺手的查询与恢复命令
4. 为后续日报与调度留稳定接口

## 范围边界

当前明确暂不做：

- 修改 `PLAN.md`
- `tokio` 迁移
- 通用网页的高质量抽取与分类
- 定时任务与日报生成
- 模式策略治理

## 实现顺序

1. 稳定浏览器 worker 在正式任务流中的调用
2. 优化公众号正文提取与 Markdown 整理
3. 增加待人工补录任务的查询入口
4. 保持微信主链路和聊天流行为不退化
5. 完成后再考虑日志分级与 `tokio` 迁移

## 当前已完成

### command_router

- 聊天流：`..` / `!!` / 超时合并
- 链接流：自动识别 URL 与 `收藏 <url>` 入库
- 命令流：`状态 <task_id>`、`最近任务`、`重试 <task_id>`

### task_store

- SQLite 初始化
- `message_dedup`
- `inbound_messages`
- `articles`
- `tasks`
- 最近任务查询
- 任务状态查询
- 任务重试
- `awaiting_manual_input`
- 任务来源、页面类型、产物路径

### pipeline

- 扫描并消费 `pending` 任务
- HTTP 抓取与浏览器抓取并存
- 对公众号正文提取 `activity-name` / `js_content`
- 生成 raw / processed / screenshot 产物
- 失败时转入 `awaiting_manual_input`，支持人工补正文

### chat_adapter

- 微信入口分流为聊天流 / 链接流 / 查询命令
- 入站文本先落库再处理
- 轮询期间顺带消费 `pending` 任务
- 支持 `补正文 <task_id> :: <content>`
- 支持 `待补录任务`
- 保留现有简单回复和文件型 Agent 能力

## 完成标准

下一阶段完成后，至少要满足：

- 发链接后任务能被后台消费
- `状态 <task_id>` 能看到状态推进、抓取来源与产物路径
- `最近任务` 能反映最新处理结果
- 聊天流、链接流、查询命令三条入口互不冲突
