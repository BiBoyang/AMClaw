# Session 2026-05-06

## 目标

完成 agent_core Phase C（C0-C8）大拆分的最终交付。

## 完成摘要

### Step 4.6（C0：建立模块骨架）

- 新建 `src/agent_core/{command_parse,context_assembly,llm_client,logging,recovery,retriever_factory,trace,types,watchdog}.rs`
- `mod.rs` 添加 `mod` 声明与 `pub(crate) use` re-export
- Gate：check + 核心测试通过

### Step 4.7（C6：LLM Client）

- 迁移 `LlmClient`, `LlmConfig`, `build_system_prompt`, `load_llm_config`, `normalize_base_url`, `clean_env`, `get_env`, `key_tail`, `is_llm_auth_error`, OpenAI response structs 到 `llm_client.rs`
- 修复 `MemoryBudget`/`StepFailureKind`/`FailureAction`/`RecoveryOutcome` 重复 `impl` 块
- 提升 `PlanningPolicy` 与 `LlmConfig` 字段为 `pub(crate)`
- `mod.rs`: 5637 行 → Gate 通过（5 LLM 测试 + 385 全量）

### Step 4.8（C7：Command Parse）

- 迁移 `LlmPlan`, `parse_user_command`, `normalize_user_command`, `parse_llm_plan`, `map_llm_plan`, `extract_json_object`, `split_path_and_content`, `impl AgentDecision`, `impl ToolAction` 到 `command_parse.rs`
- 修复未闭合分隔符、重复 `as_str`、提升 `ExecutionPlan`/`PlannedDecision` builders 为 `pub(crate)`
- `mod.rs`: 5382 行 → Gate 通过（8 测试）

### Step 4.9（C8-A：Test Extraction）

- 提取 `#[cfg(test)] mod tests { ... }`（3314 行）到 `src/agent_core/tests.rs`
- `mod.rs`: 2070 行 → 零逻辑改动 → Gate 通过（385 测试）

### Step 4.10（C8-B：Trace Models）

- 迁移 `AgentRunTrace` + 8 子 struct + 27 runtime 方法到 `trace.rs`
- 修复未闭合分隔符、重复 `truncate_for_trace`、跨模块 unresolved import、私有方法/字段访问、缺失 `Duration` import
- 提升 `PlanStepStatus`/`DEFAULT_MAX_STEPS`/`DEFAULT_MAX_REPLANS` 为 `pub(crate)`
- `mod.rs`: 1345 行 → Gate 通过（9 trace 测试）

### Step 4.11（C8-C1：Context/Watchdog Helpers）

- 迁移 `project_session_state_to_trace`, `render_context_preview` 到 `context_assembly.rs`
- 迁移 `default_minimum_novelty_for_kind` 到 `watchdog.rs`
- 提升 `ObservationKind`/`MinimumNovelty` 为 `pub(crate)`
- 修复 `tests.rs` 直接导入 `session_summary::*` 符号（移除 glob import）
- `mod.rs`: 1227 行 → Gate 通过（5 测试）

### Step 4.12（C8-C2：Logging Helpers）

- 迁移 `log_agent_info`, `log_agent_warn`, `log_agent_event`, `build_agent_log_payload` 到 `logging.rs`
- `mod.rs`: 1218 行 → Gate 通过（3 测试 + 全量）

### Step 4.13（C8-C3：Types Helpers）

- 迁移 `normalize_optional_text`, `resulting_source_name`, `observation_kind_for_action` 到 `types.rs`
- `mod.rs`: 1199 行 → Gate 通过（90 agent_core 测试）

### Step 4.14（C8-Final：收口验收与最小可见性回收）

- 结构检查：mod.rs 无已拆函数回流，submodule 分层稳定
- 最小可见性回收（trace.rs，机械调整）：
  - `truncate_for_trace` → `pub(super)`
  - `DecisionTrace`, `PromptSnapshot`, `LlmCallTrace`, `ToolCallTrace`, `FailureTrace`, `RecoveryTrace` → `pub(super)`
  - `AgentRunTrace::consecutive_failures_for_current_step` → `pub(super)`
  - `AgentRunTrace` 大量未直接访问字段 → `pub(super)`
- `cargo fmt` 修复 `tests.rs` 缩进（移除提取后多余的模块层级缩进）
- Gate：check / fmt / clippy / test 全绿

## 最终指标

| 文件 | 行数 |
|------|------|
| `src/agent_core/mod.rs` | 1193 |
| `src/agent_core/command_parse.rs` | 267 |
| `src/agent_core/context_assembly.rs` | 1499 |
| `src/agent_core/llm_client.rs` | 371 |
| `src/agent_core/logging.rs` | 22 |
| `src/agent_core/recovery.rs` | 234 |
| `src/agent_core/retriever_factory.rs` | 211 |
| `src/agent_core/tests.rs` | 3264 |
| `src/agent_core/trace.rs` | 1609 |
| `src/agent_core/types.rs` | 28 |
| `src/agent_core/watchdog.rs` | 395 |

- `mod.rs` 从 8755 → 1193 行（-86.4%）
- 全量测试：385 passed（lib）+ 36 passed（trace_eval）+ 4 passed（集成测试）

## 非机械改动清单

仅 Step 4.14 的可见性回收为非机械改动，但无行为变更：

1. `trace.rs`: `truncate_for_trace` 从 `pub(crate)` → `pub(super)`
2. `trace.rs`: `DecisionTrace`, `PromptSnapshot`, `LlmCallTrace`, `ToolCallTrace`, `FailureTrace`, `RecoveryTrace` 从 `pub(crate)` → `pub(super)`
3. `trace.rs`: `AgentRunTrace::consecutive_failures_for_current_step` 从 `pub(crate)` → `pub(super)`
4. `trace.rs`: `AgentRunTrace` 中未被 `mod.rs`/`tests.rs` 直接访问的字段从 `pub(crate)` → `pub(super)`
5. `context_assembly.rs` / `command_parse.rs`: 引用 `truncate_for_trace` 的路径从 `super::truncate_for_trace` → `super::trace::truncate_for_trace`
6. `mod.rs`: 移除 `truncate_for_trace` 的 re-export

## Gate 记录

```bash
$ cargo check
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 6.34s

$ cargo fmt --check
# 无差异

$ cargo clippy --all-targets --all-features -- -D warnings
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 13.18s

$ cargo test
    test result: ok. 385 passed; 0 failed; 0 ignored
```
