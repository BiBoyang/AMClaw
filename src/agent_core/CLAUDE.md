# AGENTS.md

@scope:src/agent_core:v1

## 模块定位

Agent 核心编排层，负责 LLM 调用、工具调度与结果整合。

## 预期职责

1. 接收上游任务上下文并调用模型。
2. 根据策略触发允许的工具链。
3. 产出结构化分类、摘要和标签结果。

## 不做事项

1. 不直接操作微信接口。
2. 不直接决定文件写入路径。
3. 不绕过模式策略执行高风险动作。
