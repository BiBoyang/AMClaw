# AGENTS.md

@scope:src/chat_adapter:v1

## 模块定位

当前微信能力实现目录，负责 iLink 登录、长轮询、消息解析与发送回复。

## 当前边界

1. 维护与 iLink 协议对接逻辑。
2. 保障接收与回复链路可运行。
3. 负责消息去重与 `context_token` 缓存。

## 演进约束

1. 新增逻辑优先保持与现有接口兼容。
2. 协议字段调整必须附带调试日志。
3. 中长期会向 `src/chat_gateway/` 结构化迁移。
