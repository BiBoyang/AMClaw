# Session 2026-04-14

## 今天目标

- 收口 Phase 4 的真实 feedback 链路
- 修复 memory 语义字典与运行时代码不一致的问题
- 完成基础校验并准备提交

## 完成内容

### 1. Phase 4 真实链路接线

- 在 `agent_core::load_business_context_snapshot` 中新增 `MemoryFeedbackState`
- 检索完成后对 `session_state.retrieved` 记录 `FeedbackKind::Retrieved`
- 注入裁剪后对 `session_state.injected` 记录 `FeedbackKind::Injected`
- 用 `apply_memory_feedback(...)` 替换旧的注入回写路径

### 2. 删除旧回写语义

- 删除 `TaskStore::mark_memory_used`
- 删除 `TaskStore::mark_memories_used`
- 更新 `agent_core` 中依赖旧语义的测试断言
- `use_count` 不再因“被注入”而增长，只在 `Useful` feedback 时增长

### 3. 同步 memory 语义字典

- 更新 `notes/context-memory/MEMORY-SEMANTIC-DICTIONARY-2026-04-13.md`
- 明确 `use_count = 被确认 useful 的次数`
- 同步检索排序规则为：
  - `priority DESC`
  - `useful DESC`
  - `use_count DESC`
  - `COALESCE(last_used_at, updated_at) DESC`
  - `id ASC`

### 4. 接入真实 Useful 产品事件

- 新增显式确认命令：`有用 <id>` / `标记有用 <id>` / `useful <id>`
- `command_router` 新增 `UserMemoryUseful`
- `chat_adapter` 接入 `handle_user_memory_useful`
- `task_store` 新增 `confirm_memory_useful(user_id, memory_id)`，校验归属后统一走 `apply_memory_feedback(...)`
- README 同步新增用户可见命令说明

## 改动文件

- `src/agent_core/mod.rs`：接入真实 feedback 记录与统一写回，更新相关测试断言
- `src/task_store/mod.rs`：删除旧的注入回写 API，保留 `apply_memory_feedback(...)` 作为唯一入口，并新增显式 useful 确认接口
- `src/command_router/mod.rs`：新增 `有用 <id>` / `useful <id>` 路由
- `src/chat_adapter/mod.rs`：新增 useful 确认接线与回归测试
- `README.md`：补充 useful 确认命令
- `notes/context-memory/MEMORY-SEMANTIC-DICTIONARY-2026-04-13.md`：收口 `use_count` / `useful` / 排序规则语义

## 校验

- [x] `cargo check`
- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets --all-features`
- [x] `cargo test`

## 风险与后续

- 风险：`Useful` 当前仅支持用户显式确认，尚无自动判定链路
- 下一步：如果后续需要自动 useful，可再定义“回复采用了哪条记忆”或“用户后续确认满意”的事件

## Context Strategy：Phase C1 计划

- 目标：引入 `ContextPack` 骨架，让 `ContextAssembler` 先组 section 再渲染
- 范围：只做结构化，不改变 prompt 行为
- 明确不做：section budget、observation hygiene、summary / compaction、`/context` 命令
- DoD：
  - 存在 `ContextSectionKind` / `ContextSection` / `ContextPack`
  - `ContextAssembler` 内部通过 `ContextPack` 渲染 prompt
  - 现有 `Current Task` / `Recent Tasks` / `User Memories` / `Available Tools` 输出保持
  - 新增最小测试覆盖 section metadata

## Context Strategy：Phase C1 进展

- `PlannerInput` 现已同时保留最终渲染 prompt 与结构化 `context_sections`
- `PromptSnapshot` / agent trace 现已保留 section 级元数据与内容摘要
- trace markdown 的 `LLM Calls` 段新增 `Context Sections`，便于后续观察各 section 实际进入 prompt 的内容
- 回归已覆盖：
  - `ContextAssembler` 产出 `latest_observation` / `current_task` / `recent_tasks` 等 section metadata
  - `cargo fmt --check`
  - `cargo test`

## Context Strategy：Phase C2-C3 今日收口

- 新增运行时 `SessionState Snapshot`，当前会从：
  - `user_input`
  - `active_plan`
  - `latest_observation`
  - `current_task`
  - controller 失败 / replan 状态
  派生出最小状态板：
  - `goal`
  - `current_subtask`
  - `constraints`
  - `confirmed_facts`
  - `done_items`
  - `next_step`
  - `open_questions`
- `ContextAssembler` 现已把 `Session State` 作为独立 section 注入 prompt
- `ContextPack` 现已引入 section policy：
  - `priority`
  - `max_chars`
  - `pinned_lines`
  - `required`
- 已落地两层预算治理：
  - section 内部超预算时执行 trim，并记录 `section_budget_exceeded`
  - 总预算超限时按优先级 drop 非必需 section，并记录 `total_budget_exceeded`
- `PromptSnapshot` / trace markdown 现已记录：
  - `context_budget_summary`
  - 各 section 的 `included / trimmed / trim_reason / drop_reason`
  - 各 section 的 `chars / original_char_count / priority / max_chars`

## 今日状态判断

- `plan / step` 主链路当前无需重写：
  - `active_plan`
  - `current_step_index`
  - `expected_observation`
  - `replan budget`
  已足够支撑当前阶段
- 今天“相对完整的上下文管理”可以定义为：
  - 有显式 `Session State`
  - 有结构化 `ContextPack`
  - 有 section budget
  - 有 dropped / trimmed reason
  - trace 可解释“本轮模型实际看到了什么”
- 尚未做：
  - older observation summary
  - session summary / compaction
  - `/context` debug 命令
  - persisted session state

## 今日校验补充

- [x] `cargo check`
- [x] `cargo fmt --check`
- [x] `cargo test`

## Context Strategy：追加推进（Observation + Debug）

- 新增 `Previous Observations` section：
  - 当前策略保留最近最多 3 条 older observations 的摘要
  - latest observation 仍单独保留完整 section
  - 目的：避免 observation 全量膨胀，同时保留最近轨迹连续性
- 新增 `AgentCore::preview_context_with_context(...)`：
  - 不走 LLM
  - 直接输出当前上下文预览
  - 包含 session state、memory 命中、budget summary、section 列表与 prompt preview
- 新增聊天命令：
  - `/context`
  - `/context <text>`
  - `上下文`
  - `上下文 <text>`
  - `context`
  - `context <text>`
- `/context` 行为：
  - 若当前用户已有 pending session，则与附加文本一起预览
  - 不消费 pending session
  - 不触发真实 agent run
  - 适合作为调试上下文装配的最小入口

## 当前可交付判断

截至今天，这一版已经可以视为“相对完整的上下文管理 v1”：

- 有显式 `Session State`
- 有结构化 `ContextPack`
- 有 section policy
- 有 section budget
- 有 trim / drop reason
- 有 latest + previous observation 策略
- 有 `/context` 调试入口
- 有 trace 级可观测

仍未做但已可后续追加：

- older observation 的摘要质量优化
- session summary / compaction
- persisted runtime session state
- 更细的 token 级 allocator

## 收尾

- README 已同步：
  - 新增 `/context [text]` / `上下文 [text]` 命令说明
  - 新增 trace 中 `context_sections / Session State / budget / trimmed / dropped reason` 说明
- 本轮收尾校验目标：
  - `cargo check`
  - `cargo fmt --check`
  - `cargo clippy --all-targets --all-features`
  - `cargo test`
- 收尾校验结果：
  - `cargo check` 通过
  - `cargo fmt --check` 通过
  - `cargo test` 通过（208 tests）
  - `cargo clippy --all-targets --all-features` 运行完成，无阻断错误，但仍有 warning
- 当前 clippy warning 主要包括：
  - 若干既有 dead_code / 未使用 helper
  - `start_llm_call` 参数偏多
  - `WriteDecision` 的 `large_enum_variant`
  - 个别测试断言与 needless borrow 建议
