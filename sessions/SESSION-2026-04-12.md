# Session 2026-04-12

## 今天目标

- 完成 PR1（状态底座）：user_memories schema 补字段、store API、migration、tests
- 如有余力开始 PR2（消费接入）：agent_core context snapshot 拼装与 memory 注入

## 完成内容

### 1. PR1 — 状态底座（已完成）

- `user_memories` 表新增 5 个字段：`memory_type`, `status`, `priority`, `last_used_at`, `use_count`
- `UserMemoryRecord` struct 同步更新
- `init_schema` 新库建完整表，老库通过 `ensure_column_exists` 自动迁移
- 新增 `add_user_memory_typed()` 支持 auto/explicit 分类写入
- 新增 `search_user_memories()` 按 priority + 时间衰减排序，去重 + 预算裁剪
- 新增 `mark_memory_used()` / `mark_memories_used()` 命中回写
- 新增 `suppress_memory()` 软删除
- `list_user_memories` 排序改为 priority DESC，过滤 status='active'
- `has_user_memory` 过滤 status='active'
- 10 个新测试覆盖：schema 字段、typed 写入、migration、检索排序去重、预算裁剪、超长跳过、命中回写、suppress、用户隔离

### 2. PR2 — 消费接入（未开始）

- 留待下一轮：agent_core 接入 search_user_memories + 命中日志 + 回写

## 改动文件

- `src/task_store/mod.rs`：schema 补字段 + migration + 4 个新 API + 10 个新测试
- `sessions/SESSION-2026-04-12.md`：当天记录

## 校验

- [x] `cargo check`
- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets --all-features`（dead_code warning 为预期，PR2 会消除）
- [x] `cargo test`（170 passed）

## 风险与后续

- 风险：新 API 尚未被消费方调用，需 PR2 接入后才能端到端验证
- 下一步：PR2（消费接入）— agent_core 的 load_business_context_snapshot 切换到 search_user_memories + 命中回写 + 结构化日志
