# Session 2026-08-26

## 今天目标

- 恢复开发：摸底 v0.3.3 真实现状，推进 S23-4 测试拆分与 Phase 4 新 Memory 类型写入入口，收口文档口径。

## 完成内容

### 1. 现状摸底

- 发现代码进度领先于文档：Phase 1（Memory v3 语义收口）代码主体此前已落地，NEXT-STEPS.md 口径落后。
- Phase 4 的存储/治理层（`MemoryType` 五枚举、优先级链、`govern_memory_write`）此前已就绪，真正缺口是写入入口。

### 2. S23-4：拆分 `agent_core` retriever_selection / rollout 测试

- 13 个测试（retriever 注入与观测 5 个、retriever mode 选择 5 个、rollout 回退 3 个）纯移动到 `src/agent_core/tests/retriever_selection.rs`（490 行）。
- `src/agent_core/tests.rs` 从 1997 → 1516 行；拆分前后 `cargo test --lib agent_core::tests` 均 91 passed。
- 未修改生产代码，未修改断言语义。

### 3. Phase 4：新 Memory 类型写入入口

- 自动提炼类型归位：`extract_auto_memory_candidate` 偏好类触发词 → `UserPreference`，主题/在做类触发词 → `ProjectFact`（`src/chat_adapter/command_handlers.rs`）。
- DB 内容保留 `偏好: `/`主题: ` 文本前缀，保证与历史 auto 记忆的 dedup/promote 连续性；prompt 渲染侧新增 `strip_redundant_type_prefix` 去双重标注（`src/agent_core/context_assembly.rs`）。
- Lesson 沉淀：新增 `persist_failure_lesson`，接在 `run_with_context` 的 Err 分支，best-effort 不影响主链路（`src/agent_core/mod.rs`）。
- 新增 6 个测试（chat_adapter 3 个 + agent_core memory_context 3 个）。

### 4. 文档口径收口

- 语义字典补全五类型定义、优先级链与 5 个新日志事件（`user_memory_auto_promoted`、`memory_lesson_*`）。
- NEXT-STEPS.md：Phase 1 / Phase 4 标记完成；Phase 5 标注真实 trace 样本不足（约 5 条）的 blocker，明确当前最优先是积累真实样本。
- README 无需同步：无用户可见命令/行为变化。

## 改动文件

- `src/agent_core/tests.rs` / `src/agent_core/tests/retriever_selection.rs`：S23-4 测试拆分。
- `src/chat_adapter/command_handlers.rs` / `src/chat_adapter/tests.rs`：自动提炼类型归位 + 测试。
- `src/agent_core/mod.rs`：`persist_failure_lesson` 与 Err 分支接线。
- `src/agent_core/context_assembly.rs`：`strip_redundant_type_prefix`。
- `src/agent_core/tests/memory_context.rs`：lesson 沉淀测试。
- `notes/context-memory/MEMORY-SEMANTIC-DICTIONARY-2026-04-13.md`：五类型与新日志事件口径。
- `NEXT-STEPS.md`：Phase 1/4 收口、Phase 5 blocker 标注。
- `sessions/SESSION-2026-08-26.md`、`sessions/README.md`：本记录与索引。

## 校验

- [x] `cargo check`
- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets --all-features`（仅 2 个历史遗留 warning，位于未触碰文件）
- [x] `cargo test --lib`（406 passed, 0 failed）

## 评测摘要

- 本轮改了 memory 写入链路的类型归位与 lesson 沉淀，但未改检索/注入预算与评测口径，未运行 `trace_eval`。
- Phase 5 评测闭环等待真实 trace 样本积累（当前约 5 条）。

## 风险与后续

- 风险：改了 `chat_adapter` 写入链路，属 AGENTS.md"大改动"边缘，**尚未做人工扫码回归**（登录/收发/去重/优雅退出需用户执行）。
- 遗留：EscalatedToAskUser 收场不沉淀 lesson；环境性失败（如 LLM 401）也会沉淀 lesson，靠 dedup 兜底。
- 下一步：Phase 5 —— 让 bot 真实跑起来积累 trace 样本（目标 20~50 条），到位后做五类语义失败的标注 schema 与 trace_eval 聚合。

---

# 下半场：Phase 5 第一刀（标注基建 + Wilson 置信界 + 重校纪律）

> 起因：用户对撞 `trace-eval-v2.md` 研究稿（假通过/假失败归因学、judge 校准、统计口径），确认 AMClaw 即文中"第三个项目"，决定把 V2 外部对撞带回的方法论搬进 Phase 5。

## 完成内容

### 5. trace_eval 报告层带分母 + 单侧 99% Wilson 置信界

- 9 个比例类 gate 指标输出 `rate (n/d), Wilson99 上界/下界` 形态（HiB 看下界、LoB 看上界，z=2.326）；`avg_step_count` 为均值不加区间。
- 逐指标核对分母口径（recovery 分母是 attempts 不是 runs 等）。
- compare 反解析兼容新旧报告格式，旧 baseline 行为不变；**gate 判定逻辑未动**（分两步走，先让数字诚实）。
- 改动仅 `src/bin/trace_eval.rs`；新增 5 个测试（Wilson 算例对照、边界、新旧格式解析回归）。

### 6. 语义失败标注基建

- 新增 `notes/agent-eval/specs/SEMANTIC-FAILURE-ANNOTATION-GUIDE-2026-08-26.md`：两层标注（先 `not_agent_fault` 筛假失败，再定五类二元主标签）、易混边界判定、先依据后判定、`uncertain` 退路、带分母纪律、criteria drift 条款。
- 新增 `notes/agent-eval/annotations/SEMANTIC-FAILURE-ANNOTATIONS-v1.md` 批次模板（待样本量启用）。

### 7. 重校纪律文档化

- `GATE-POLICY-SPEC-2026-05-08.md` 新增"重校纪律"一节：模型/干预变更即重校、标注集保质期、n<20 不报点估计结论、假失败对称面。
- `notes/agent-eval/README.md` 注册 annotations/ 与新指引；NEXT-STEPS.md Phase 5 三项标 ✅。

## 改动文件（下半场）

- `src/bin/trace_eval.rs`：Wilson 界 + 分母输出 + 解析兼容。
- `notes/agent-eval/specs/SEMANTIC-FAILURE-ANNOTATION-GUIDE-2026-08-26.md`、`notes/agent-eval/annotations/SEMANTIC-FAILURE-ANNOTATIONS-v1.md`：新增。
- `notes/agent-eval/specs/GATE-POLICY-SPEC-2026-05-08.md`、`notes/agent-eval/README.md`、`NEXT-STEPS.md`：口径同步。
- `notes/agent-eval/reports/TRACE-EVAL-REPORT.md`、`TRACE-EVAL-COMPARE.md`：trace_eval 实跑滚动更新。

## 校验（下半场）

- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets --all-features`（仅 2 个历史遗留 warning）
- [x] `cargo test` 全量（lib 406 + trace_eval bin 57，全绿）
- [x] `./scripts/trace_compare.sh` 实跑：旧 baseline 解析不回归，compare 10 项 PASS

## 评测摘要（下半场）

- 本轮改的是评测工具自身的报告口径，gate 判定语义未变；compare 实跑综合判定 PASS。

## 风险与后续（下半场）

- 风险：低。改动集中在离线评测工具与文档，不碰主链路，无需人工回归。
- 已知债务：compare 反解析 markdown 报告的架构较脆（格式耦合加深一层），后续可考虑结构化中间产物。
- 下一步不变：bot 真实跑起来攒 trace，20+ 条后按指引启动第一批语义失败标注。
